{
  "name": "auto-update-app",
  "version": "1.0.1",
  "description": "boilerplate for auto update",
  "main": "main.js",
  "scripts": {
    "start": "electron ./src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
"deploy": "electron-builder build --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Galionix/auto-update-app-boilerplate.git"
  },
  "keywords": [
    "template",
    "boilerplate"
  ],
  "author": "dimas",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Galionix/auto-update-app-boilerplate/issues"
  },
  "homepage": "https://github.com/Galionix/auto-update-app-boilerplate#readme",
  "devDependencies": {
    "electron": "^11.0.3",
    "electron-builder": "^22.9.1"
  },
  "dependencies": {
    "electron-updater": "^4.3.5"
  }
 

}
